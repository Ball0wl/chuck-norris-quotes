import React from 'react';

const Header = () => {
    return (
        <header className="header-container">
            <img src="/image/logo.png" alt="W&S Logo"/>
        </header>
    );
};

export default Header;